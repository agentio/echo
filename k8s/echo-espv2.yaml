apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo
  namespace: terminate-me
spec:
  replicas: 1
  selector:
    matchLabels:
      app: echo
  template:
    metadata:
      labels:
        app: echo
    spec:
      volumes:
        - name: service-account-creds
          secret:
            secretName: service-account-creds
      containers:
      - name: esp
        image: ghcr.io/terminate-me/esp-v2:latest
        args: [
          "--listener_port=8081",
          "--backend=127.0.0.1:8080",
          "--service=echo-api.endpoints.terminate-me.cloud.goog",
          "--rollout_strategy=managed",
          "--non_gcp",
          "--service_account_key=/etc/esp/creds/service-account-creds.json"
        ]
        ports:
          - containerPort: 8081
        volumeMounts:
          - mountPath: /etc/esp/creds
            name: service-account-creds
            readOnly: true
      - name: echo
        image: ghcr.io/terminate-me/echo-server:latest
        ports:
          - containerPort: 8080    
      nodeSelector:
        kubernetes.io/arch: amd64
---
apiVersion: v1
kind: Service
metadata:
  name: echo
  namespace: terminate-me
spec:
  ports:
  - port: 80
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: echo
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: echo
  namespace: terminate-me
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
  - hosts:
      - zulu.timbx.me
    secretName: zulu-tls
  rules:
  - host: zulu.timbx.me
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: echo
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: echo-google
  namespace: terminate-me
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  tls:
  - hosts:
      - echo-api.endpoints.terminate-me.cloud.goog
    secretName: echo-api-tls
  rules:
  - host: echo-api.endpoints.terminate-me.cloud.goog
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: echo
            port:
              number: 80